<section class="hero-video-section">
  <video
    class="hero-video-bg"
    src="/AccueilHD.mp4"
    autoplay
    muted
    loop
    playsinline
    preload="auto"
  ></video>
  <div class="hero-video-overlay"></div>
  <div class="hero-video-content">
    <h1>Bienvenue au Restaurant du Lac de Bret</h1>
    <p class="hero-subtitle">Une aventure culinaire familiale à Puidoux depuis 1979</p>
    <div class="hero-video-buttons">
      <a href="tel:+41219461126" class="btn btn-primary">Réserver une table</a>
      <a href="#menu" class="btn btn-secondary">Voir notre menu de saison</a>
    </div>
  </div>
</section>

<style>
.hero-video-section {
  position: relative;
  width: 100vw;
  max-width: 100%;
  aspect-ratio: 1920 / 822;
  height: auto;
  min-height: 300px;
  overflow: hidden;
}

.hero-video-bg {
  position: absolute;
  top: 0; left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  z-index: 1;
  background: #000;
  pointer-events: none;
}

.hero-video-overlay {
  position: absolute;
  top: 0; left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, rgba(0,0,0,0.45) 0%, rgba(0,0,0,0.25) 100%);
  z-index: 2;
  pointer-events: none;
}

.hero-video-content {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 100%;
  max-width: 900px;
  z-index: 3;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #fff;
  text-align: center;
  text-shadow: 0 2px 8px rgba(0,0,0,0.5);
  padding: 2rem;
  box-sizing: border-box;
}

.hero-video-content h1 {
  font-size: 4rem;
  font-weight: 900;
  letter-spacing: -1px;
  margin-bottom: 1.2rem;
  line-height: 1.1;
  text-shadow: 0 4px 24px rgba(0,0,0,0.7), 0 1px 2px rgba(0,0,0,0.3);
}

.hero-subtitle {
  font-size: 1.3rem;
  font-weight: 400;
  opacity: 0.85;
  margin-bottom: 2.5rem;
  color: #fff;
  text-shadow: 0 2px 8px rgba(0,0,0,0.5);
}

.hero-video-buttons {
  display: flex;
  gap: 1.2rem;
  flex-wrap: wrap;
  justify-content: center;
}

.btn {
  display: inline-block;
  font-size: 1.15rem;
  font-weight: 700;
  padding: 1rem 2.2rem;
  border-radius: 0.6rem;
  text-decoration: none;
  transition: background 0.2s, color 0.2s, border 0.2s;
  box-shadow: 0 2px 12px rgba(0,0,0,0.12);
  cursor: pointer;
}

.btn-primary {
  background-color: var(--primary-green);
  color: var(--white);
  border: 2px solid var(--primary-green);
  border-radius: 0.5rem;
  padding: 0.6rem 1.5rem;
  font-weight: 600;
  box-shadow: 0 4px 24px rgba(127,176,105,0.15);
  transition: background 0.3s, color 0.3s, border 0.3s, transform 0.2s, box-shadow 0.2s;
}
.btn-primary:hover,
.btn-primary:focus {
  background-color: var(--accent-green);
  color: var(--white);
  border-color: var(--accent-green);
  transform: translateY(-2px);
  box-shadow: 0 4px 15px rgba(127,176,105,0.4);
}

.btn-secondary {
  background: transparent;
  color: #fff;
  border: 2px solid #fff;
  box-shadow: none;
}
.btn-secondary:hover,
.btn-secondary:focus {
  background: rgba(255,255,255,0.12);
  color: #76FF4B;
  border-color: #76FF4B;
}

@media (max-width: 1200px) {
  .hero-video-content h1 { font-size: 2.5rem; }
  .hero-subtitle { font-size: 1.1rem; }
  .btn { font-size: 1rem; padding: 0.8rem 1.5rem; }
}

@media (max-width: 768px) {
  .hero-video-content h1 {
    font-size: 2.5rem;
    font-weight: bold;
    line-height: 1.15;
    padding: 0 0.2em;
  }
}

@media (max-width: 600px) {
  .hero-video-section {
    aspect-ratio: unset;
    height: 80vh;
    min-height: 180px;
  }
  .hero-video-bg {
    height: 80vh;
    object-position: top;
  }
  .hero-video-content {
    padding: 1rem;
    max-width: 98vw;
  }
  .hero-video-content h1 {
    font-size: 2rem;
    line-height: 1.15;
    padding: 0 0.2em;
    font-weight: bold;
  }
  .hero-subtitle {
    font-size: 0.95rem;
    margin-bottom: 1.5rem;
  }
  .btn { font-size: 0.95rem; padding: 0.7rem 1.1rem; }
}
</style> 